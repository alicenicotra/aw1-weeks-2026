"use strict";

// oggetto
const movie = {
    title: "Avatar",
    genre: "Sci-fi",
    duration: 180,
} 

console.log(movie);                                                     // { title: 'Avatar', genre: 'Sci-fi', duration: 180 }
console.log(`L'unico film di questo programma è: ${movie}`);            // L'unico film di questo programma è: [object Object]
console.log(`L'unico film di questo programma è: ${movie.title}`);      // L'unico film di questo programma è: Avatar


