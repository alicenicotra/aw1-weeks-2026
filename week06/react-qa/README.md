# Components

* Navbar -> question.id
* Description -> question.id, question.author, question.text
* Answers -> question.answers
    * Table -> question.answers
        * TableRow -> question.ans
            * TableElement -> specific field
            * Actions
* Footer